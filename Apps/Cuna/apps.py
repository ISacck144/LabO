from django.apps import AppConfig


class CunaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Apps.Cuna' # Ensure the name matches the directory structure
    
